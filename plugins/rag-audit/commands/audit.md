---
description: Run a structural audit of the RAG pipeline and produce a linter-style report with rule findings and a 0-100 structure score
argument-hint: [path]
---

Invoke the `rag-audit` skill (via the Skill tool) and follow its workflow exactly — full rule coverage, the score computation, and the report template.

Audit target: $ARGUMENTS

If no target was given above, audit the current repository.
